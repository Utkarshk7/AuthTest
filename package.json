{
  "name": "project",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "@sap/approuter": "14.4.2",
    "@sap/html5-repo-mock": "2.1.5"
  },
  "scripts": {
    "build-AuthTest": "cd AuthTest && if [ ! -d ${workspaceFolder}/AuthTest/AuthTest/node_modules/react-scripts ] && [ ! -d ${workspaceFolder}/AuthTest/node_modules/react-scripts ]; then npm i; fi && npm run build"
  }
}
